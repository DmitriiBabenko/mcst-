//
// Created by Dmitrii B. on 19.08.2026
//
#pragma once
#include <vector>
#include "Graph.h"

/* 
Этап 4: отображение вершин на регистры rho: V -> {0, ..., regs-1}.
pred: graph - граф зависимостей в топологически согласованном порядке (индекс вершины возрастает вдоль
        рёбер E, т.е. этап 3 выполнен); gen - псевдослучайный генератор (для случайного выбора свободного
        регистра из лдоступных); regs - число доступных регистров
post: тот же граф (ops/ways/incWays/values без изменений) с заполненным rho: каждой вершине назначен
        регистр, свободный на момент её вычисления; регистр операнда освобождается сразу после последнего
        узла, которому этот операнд ещё нужен как исходное значение; бросает std::runtime_error, если для очередной
        вершины не осталось свободных регистров (regs недостаточно для данного графа)
*/
const Graph assignComponent(const Graph & graph, std::mt19937 & gen, const unsigned & regs);