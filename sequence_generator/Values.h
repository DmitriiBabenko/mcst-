#pragma once
#include <vector>
#include "Graph.h"

// Этап 2: формирование и разрешение системы ограничений Z3 в соответствии с построенным графов.
// pred: graph - граф зависимостей G=(V,E) с построенными ops/ways/incWays (этап 1 выполнен);
//      для каждой не-INIT вершины incWays содержит ровно два операнда;
//      seed - параметр random_seed солвера Z3 (для детерминированности); log - выводить ли построенные ограничения в stdout
// post: вектор значений values[v] для каждой вершины v из V, удовлетворябщих системе ограничений
//      (в формате IEEE-754 float32: v = f(v)(операнды), значения из [-1000, 1000] \ (-0.0001, 0.0001),
//      делитель в DIV не равен 0); бросате std::runtime_error, если система не выполнима (unsat)
std::vector<float> solve(const Graph & graph, unsigned seed, const bool & log);