#pragma once
#include <vector>
#include "Graph.h"

// Этап 3: построение топологического порядка sigma: V -> {1, ..., |V|}, согласованного с E
// (для всех рёбер (v_i, v_j) из E: sigma(v_i) < sigma(v_j)).
// pred: graph - набор графов зависимостей (уже с построенными ops/ways/incWays, этап 1 выполнен;
//      values, если есть, входят в результат без изменений); seed - для детерминированного случайного
//      выбора порядка обхода среди топологически допустимых; log - выводить ли результат каждого графа
// post: те же графы, но с вершинами, переиндексированными по некоторому топологическому порядку sigma
//       (индекс вершины idx в результирующем Graph равен sigma(v) - 1 для исходной вершины v); ways/incWays
//       пересчитаны под новую индексацию, regs каждого графа сохраняется как есть
const std::vector<Graph> topSort(const std::vector<Graph> & graph, const unsigned & seed, const bool & log);